{
  "name": "lianyaohu-website",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:prepare": "mkdir -p website/public && cp scripts/install.sh scripts/uninstall.sh website/public/",
    "docs:dev": "npm run docs:prepare && vitepress dev website",
    "docs:build": "npm run docs:prepare && vitepress build website",
    "docs:preview": "vitepress preview website"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
